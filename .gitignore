runs/
models/
__pycache__/
*.py[cod]
*.jsonl
.pytest_cache/
.DS_Store
*.egg-info/
build/
dist/
.cache/

# Trang tài liệu gộp — sinh lại bằng `make site`, đừng commit bản build.
# (Bỏ dòng này nếu bạn muốn phục vụ nó qua GitHub Pages.)
docs/site.html

# Kết quả chạy và mesh tải về
assets/meshy/tasks.json
.venv/
venv/

# KHOÁ API — không bao giờ vào kho. Đọc qua biến môi trường, xem genesis/genai.py.
.env
.env.*
!.env.example
