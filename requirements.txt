# Genesis Zero — phụ thuộc lõi. Đủ để chạy ván, server, và client.
#
#   pip install -r requirements.txt
#
# Thêm tính năng thì cài nhóm phụ (xem pyproject.toml):
#   pip install -e ".[analysis]"   biểu đồ X-05, kiểm định X-02
#   pip install -e ".[train]"      huấn luyện R-03 (torch, nặng)
#   pip install -e ".[viz]"        cửa sổ xem ván (pygame)
#   pip install -e ".[dev]"        pytest
rich>=13
httpx>=0.27
fastapi>=0.115
uvicorn>=0.27
pydantic>=2
